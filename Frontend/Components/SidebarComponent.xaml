<UserControl x:Class="ProjectBReadyWPF.Frontend.Components.SidebarComponent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="250">
    
    <Border Background="#2C3E50">
        <StackPanel Margin="10,30,10,10">
            <!-- Project Logo -->
            <Image Source="pack://application:,,,/Frontend/Assets/logo.jpg" Height="80" Width="80" Margin="0,0,0,10" Stretch="UniformToFill"/>
            <TextBlock Text="B-READY" Foreground="White" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,5"/>
            <TextBlock Text="BARANGAY DISASTER RELIEF" Foreground="#BDC3C7" FontSize="10" HorizontalAlignment="Center" Margin="0,0,0,30"/>

            <Button x:Name="BtnDashboard" Content="Main Dashboard" Margin="0,5" Height="40" Background="#34495E" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            
            <!-- These three are hidden in Kiosk/Resident Mode -->
            <Button x:Name="BtnShelter" Content="Shelter Management" Margin="0,5" Height="40" Background="#34495E" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            <Button x:Name="BtnInventory" Content="Relief Inventory" Margin="0,5" Height="40" Background="#34495E" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            <Button x:Name="BtnReport" Content="Situational Reports" Margin="0,5" Height="40" Background="#34495E" Foreground="White" BorderThickness="0" Cursor="Hand"/>
        </StackPanel>
    </Border>
</UserControl>